'use strict';

/**
 * invitations-feedback service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::invitations-feedback.invitations-feedback');
